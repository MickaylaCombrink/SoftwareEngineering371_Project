/**
 * PERSON 3 — Shopping Cart API.  NOT IMPLEMENTED.
 *
 * Suggested route table (every route needs Person 2's `protect`):
 *   GET    /
 *   POST   /items
 *   PUT    /items/:productId
 *   DELETE /items
 *
 * Once the handlers exist, uncomment the /api/cart mount in src/app.js.
 */
const express = require('express');

const router = express.Router();

// TODO: router.use(protect); then the routes above.

module.exports = router;
