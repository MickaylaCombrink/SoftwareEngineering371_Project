/**
 * UNASSIGNED — Orders.  NOT IMPLEMENTED.
 *
 * Handlers to write: createOrder (checkout), getMyOrders, getOrder,
 * updateOrderStatus (admin only).
 *
 * Test case from the plan: requesting another user's order by id -> 403,
 * with no order data in the response.
 */

module.exports = {};
