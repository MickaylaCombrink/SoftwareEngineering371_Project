/**
 * PERSON 3 — Shopping Cart API.  NOT IMPLEMENTED.
 *
 * HTTP concerns only — status codes and response shape. Put the rules in
 * src/services/cartService.js.
 *
 * Handlers to write: getCart, addItem, updateItemQuantity, removeItem.
 *
 * req.user is populated by Person 2's `protect` middleware, so this
 * cannot be finished until that lands.
 *
 * Wrap every handler in catchAsync:
 *   const catchAsync = require('../utils/catchAsync');
 */

module.exports = {};
